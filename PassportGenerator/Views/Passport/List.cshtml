@using PassportGenerator.Models
@model IEnumerable<PassportGenerator.Models.Passport>

@{
    ViewBag.Title = "List";
}

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            
            background-image: url('../../imgs/window.jpg');
            background-size: cover;
            background-position: inherit;
            position: relative;
        }

            body::before {
                content: "";
                background: inherit;
                filter: blur(20px); 
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                z-index: -1;
            }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="mt-5 mb-3">Passport List</h1>
        <table class="table table-striped">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">Passport ID</th>
                    <th scope="col">Profile Picture</th>
                    <th scope="col">Birth Proof</th>
                    <th scope="col">Identity Proof</th>
                    <th scope="col">Nationality</th>
                    <th scope="col">Singnature</th>
                    <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
                @*@foreach (Passport passport in Model)
                    {
                        <tr>
                            <td>@passport.Id</td>
                            <td>
                                <img class="rounded float-start img-fluid" src="data:image/png;base64,@Convert.ToBase64String(passport.PhotoBytes)" style="height:100px;width:100px;" />
                            </td>
                            <td>
                                <a href="data:application/pdf;base64,@Convert.ToBase64String(passport.BirthProofBytes)" download>Birth Proof</a>
                            </td>
                            <td>
                                <a href="data:application/pdf;base64,@Convert.ToBase64String(passport.IdentityProofBytes)" download>Identity Proof</a>
                            </td>
                            <td>
                                <a href="data:application/pdf;base64,@Convert.ToBase64String(passport.NationalityProofBytes)" download>Nationality</a>
                            </td>
                            <td>
                                <img class="rounded float-start img-fluid" src="data:image/png;base64,@Convert.ToBase64String(passport.SignatureBytes)" style="height:100px;width:100px;" />
                            </td>
                            <td>
                                @Html.ActionLink("Edit", "Edit", new { id = passport.Id }, new { @class = "btn btn-warning btn-sm" })
                                @Html.ActionLink("Delete", "Delete", new { id = passport.Id }, new { @class = "btn btn-danger btn-sm" })
                            </td>
                        </tr>
                    }*@
                @*@foreach (Passport passport in Model)
                    {
                        <tr>
                            <td>@passport.Id</td>
                            <td>
                                <img class="rounded float-start img-fluid" src="data:image/png;base64,@Convert.ToBase64String(passport.PhotoBytes)" style="height:100px;width:100px;" />
                            </td>
                            <td>
                                 Birth Proof PDF
                                <a href="javascript:void(0);" onclick="openPdf('@Convert.ToBase64String(passport.BirthProofBytes)')">
                                    <iframe src="data:application/pdf;base64,@Convert.ToBase64String(passport.BirthProofBytes)" style="border: none; width: 100px; height: 100px;"></iframe>
                                </a>
                            </td>
                            <td>
                                 Identity Proof PDF
                                <a href="javascript:void(0);" onclick="openPdf('@Convert.ToBase64String(passport.IdentityProofBytes)')">
                                    <iframe src="data:application/pdf;base64,@Convert.ToBase64String(passport.IdentityProofBytes)" style="border: none; width: 100px; height: 100px;"></iframe>
                                </a>
                            </td>
                            <td>
                                 Nationality Proof PDF
                                <a href="javascript:void(0);" onclick="openPdf('@Convert.ToBase64String(passport.NationalityProofBytes)')">
                                    <iframe src="data:application/pdf;base64,@Convert.ToBase64String(passport.NationalityProofBytes)" style="border: none; width: 100px; height: 100px;"></iframe>
                                </a>
                            </td>
                            <td>
                                <img class="rounded float-start img-fluid" src="data:image/png;base64,@Convert.ToBase64String(passport.SignatureBytes)" style="height:100px;width:100px;" />
                            </td>
                            <td>
                                @Html.ActionLink("Edit", "Edit", new { id = passport.Id }, new { @class = "btn btn-warning btn-sm" })
                                @Html.ActionLink("Delete", "Delete", new { id = passport.Id }, new { @class = "btn btn-danger btn-sm" })
                            </td>
                        </tr>
                    }*@


                @foreach (Passport passport in Model)
                {
                    <tr>
                        <td>@passport.Id</td>
                        <td>
                            <img class="rounded float-start img-fluid" src="data:image/png;base64,@Convert.ToBase64String(passport.PhotoBytes)" style="height:100px;width:100px;" />
                        </td>
                        <td>
                            <!-- Birth Proof PDF -->
                            <a href="javascript:void(0);" onclick="openPdf('@Convert.ToBase64String(passport.BirthProofBytes)')">
                                <iframe src="data:application/pdf;base64,@Convert.ToBase64String(passport.BirthProofBytes)" style="border: none; width: 100px; height: 100px;"></iframe>
                            </a>
                        </td>
                        <td>
                            <!-- Identity Proof PDF -->
                            <a href="javascript:void(0);" onclick="openPdf('@Convert.ToBase64String(passport.IdentityProofBytes)')">
                                <iframe src="data:application/pdf;base64,@Convert.ToBase64String(passport.IdentityProofBytes)" style="border: none; width: 100px; height: 100px;"></iframe>
                            </a>
                        </td>
                        <td>
                            <!-- Nationality Proof PDF -->
                            <a href="javascript:void(0);" onclick="openPdf('@Convert.ToBase64String(passport.NationalityProofBytes)')">
                                <iframe src="data:application/pdf;base64,@Convert.ToBase64String(passport.NationalityProofBytes)" style="border: none; width: 100px; height: 100px;"></iframe>
                            </a>
                        </td>
                        <td>
                            @if (passport.SignatureBytes != null)
                            {
                                <img class="rounded float-start img-fluid" src="data:image/png;base64,@Convert.ToBase64String(passport.SignatureBytes)" style="height:100px;width:100px;" />
                            }
                            else
                            {
                                <span>No Signature</span>
                            }
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = passport.Id }, new { @class = "btn btn-warning btn-sm" })
                            @Html.ActionLink("Delete", "Delete", new { id = passport.Id }, new { @class = "btn btn-danger btn-sm" })
                        </td>
                    </tr>
                }

            </tbody>
        </table>
    </div>

    <script>
        function openPdf(base64Pdf) {
            // Create a Blob object from the base64 PDF data
            var byteCharacters = atob(base64Pdf);
            var byteNumbers = new Array(byteCharacters.length);
            for (var i = 0; i < byteCharacters.length; i++) {
                byteNumbers[i] = byteCharacters.charCodeAt(i);
            }
            var byteArray = new Uint8Array(byteNumbers);
            var blob = new Blob([byteArray], { type: 'application/pdf' });

            // Create a URL for the Blob object
            var blobUrl = URL.createObjectURL(blob);

            // Open the PDF in a new tab
            window.open(blobUrl, '_blank');
        }
    </script>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/2.5.3/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
































